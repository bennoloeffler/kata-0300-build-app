(ns kata-0300-build-app.core-test
  #_{:clj-kondo/ignore [:refer-all]}
  (:require [clojure.test :refer :all]
            [kata-0300-build-app.core :refer :all]))

(deftest a-test
  (testing "should run..."
    (is (= 1 1))))
